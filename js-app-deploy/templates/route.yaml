apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    name: {{ .Values.name }}
    template: js-app-deploy-template
  name: {{ .Values.name }}
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: {{ .Values.name }}
    weight: 100
  wildcardPolicy: None
